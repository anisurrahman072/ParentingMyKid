/**
 * Plan reference name for the parent appearance / premium theme screen.
 * Implementation lives in `appearance.tsx` (gradients, optional custom background in future).
 */
export { default } from './appearance';
