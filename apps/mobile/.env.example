# Optional: override API base (include /api/v1). Use when Expo cannot infer your PC IP
# (e.g. tunnel mode) or the backend runs on another host.
# EXPO_PUBLIC_API_BASE_URL=http://192.168.0.50:3000/api/v1
